rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes go through the Admin SDK (server-side).
    // No client should ever touch Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
